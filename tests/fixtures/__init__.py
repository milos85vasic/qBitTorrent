"""Shared pytest fixtures for qBittorrent-Fixed tests.

Submodules:
    services: live service-availability fixtures (merge service, qBittorrent
        WebUI proxy, webui-bridge). Fail with actionable messages instead of
        silently skipping when services are down — see Phase 0.3.
"""
