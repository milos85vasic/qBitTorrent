# Test + developer tooling for qBittorrent-Fixed.
# See pyproject.toml for tool configuration and
# docs/superpowers/plans/2026-04-19-completion-initiative.md for rationale.

# --- HTTP clients ---
requests>=2.32.0
httpx>=0.27.0
aiohttp>=3.9.0

# --- Test runner + plugins ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0        # global --timeout hard cap
pytest-randomly>=3.15.0      # random test order exposes hidden coupling
pytest-xdist>=3.6.0          # parallel test execution
pytest-docker>=3.1.0         # compose up/down fixtures
pytest-benchmark>=4.0.0      # performance tests
pytest-repeat>=0.9.3         # repeat flaky tests to prove stability

# --- Mocking / fixtures ---
responses>=0.25.0            # requests mocking
respx>=0.21.0                # httpx mocking
freezegun>=1.5.0             # time freezing
hypothesis>=6.103.0          # property-based testing

# --- Static analysis / scanners run in CI via docker-compose.quality.yml ---
ruff>=0.6.0
mypy>=1.11.0
bandit[toml]>=1.7.9
pip-audit>=2.7.3

# --- Link checking for docs ---
linkchecker>=10.4.0

# --- Coverage & mutation ---
coverage[toml]>=7.5.0
mutmut>=2.5.0

# --- Runtime libraries exercised by tests (must be installable locally too) ---
pydantic>=2.7.0
cachetools>=5.3.0           # TTLCache for Phase 3 hardening
filelock>=3.15.0            # file-locking for credential writes
tenacity>=8.4.0             # retry/backoff
pybreaker>=1.2.0            # circuit breaker

# --- Browser e2e (optional; install with `pip install playwright && playwright install chromium`) ---
playwright>=1.44.0
pytest-playwright>=0.5.0

# --- Load / stress ---
locust>=2.29.0

# --- API contract ---
schemathesis>=3.33.0
