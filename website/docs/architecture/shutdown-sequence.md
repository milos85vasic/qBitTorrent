# Shutdown Sequence

Orderly shutdown flow across containers, the webui-bridge, and in-flight
search/download state.

```mermaid
{% include-markdown "../../../docs/architecture/shutdown-sequence.mmd" %}
```
